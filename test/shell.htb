BLOCK setShellCommandVars
_IF "$USER" MATCH ".USER"
  # not a unix, assume is windows
  _SET CAT=type
  _SET RM=del
  _SET LS=dir
  _SET DEV_NULL=nul
_ELSE
  # some unix
  _SET CAT=cat
  _SET RM=rm
  _SET LS=ls
  _SET DEV_NULL=/dev/null
_END
END
